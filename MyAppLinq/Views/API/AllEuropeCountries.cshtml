
@{
    ViewBag.Title = "AllEuropeCountries";
}

<h2>AllEuropeCountries</h2>
<ul>
    @foreach (var country in ViewBag.Countries)
    {
        <li><a href=@Url.Action("Country", "API", new { name = country.name })>@country.name</a></li>
    }
</ul>

